# Keep secret API keys out of GitHub — NEVER commit this
.streamlit/secrets.toml

# Python junk
__pycache__/
*.pyc

# Local World Cup result cache — rebuilt from the API by research/wc_ingest.py
research/wc_matches.jsonl

# Large PNG image originals — we commit the compressed .jpg versions instead
images/*.png
images/banners/*.png
